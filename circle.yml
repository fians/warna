machine:
  pre: 
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.24.1/install.sh | bash
    - nvm install 0.10.38
    - nvm install 0.12.2

dependencies:
  override:
    - nvm use 0.10.38 && npm install
    - nvm use 0.12.2 && npm install
    
